package capture

import (
	"github.com/databeast/goatherd/packets"
)

// Process a single Packet summary from this Capturepoint
func (c *CapturePoint) process(summary packets.PacketSummary) {

	// is the packet source on the same subnet as the capturepoint?

	// is the packet source on a viable subnet of the capturepoint

	// flag the variant bits beyond the mask bits.

}
